[build]
builder = "nixpacks"

[deploy]
startCommand = "node orchestrator.js"
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[service]
internalPort = 3001
